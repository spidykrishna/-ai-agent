services:
  - type: web
    name: ai-calling-agent
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: gunicorn ai_calling_agent.wsgi:application